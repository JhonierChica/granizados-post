// Navbar replaced by TopBar — see components/layout/TopBar.tsx
import React from 'react';

// Keep as a no-op to avoid breaking any residual imports
const Navbar: React.FC = () => null;

export default Navbar;
